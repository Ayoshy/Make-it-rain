<ResourceDictionary x:Class="Battlestation.GlassMenus" x:ClassModifier="internal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Battlestation">
    <!-- One template family for WPF context menus, dropdowns and their submenus. -->
    <LinearGradientBrush x:Key="MenuGlass" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{local:ThemeColor #EB44344F}" Offset="0"/>
        <GradientStop Color="{local:ThemeColor #E62C213B}" Offset="0.4"/>
        <GradientStop Color="{local:ThemeColor #F01F192C}" Offset="1"/>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="MenuRim" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{local:ThemeColor #A6F1E4FF}" Offset="0"/>
        <GradientStop Color="{local:ThemeColor #35DACDEC}" Offset="0.45"/>
        <GradientStop Color="{local:ThemeColor #729DCEDA}" Offset="1"/>
    </LinearGradientBrush>
    <Style x:Key="MenuPlate" TargetType="Border">
        <Setter Property="CornerRadius" Value="15"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="Background" Value="{StaticResource MenuGlass}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MenuRim}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style TargetType="ScrollBar">
        <Setter Property="MinWidth" Value="14"/>
        <Setter Property="MinHeight" Value="14"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent">
                        <Border x:Name="Rail" Margin="3" CornerRadius="4" Background="{local:ThemeBrush #18E8D9F6}"/>
                        <Track x:Name="PART_Track" IsDirectionReversed="True" Orientation="{TemplateBinding Orientation}">
                            <Track.DecreaseRepeatButton><RepeatButton Command="ScrollBar.PageUpCommand" Background="Transparent" BorderThickness="0"/></Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Cursor="Hand">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border x:Name="Thumb" Margin="2" CornerRadius="5" BorderThickness="1" BorderBrush="{local:ThemeBrush #9CDDD0EE}" Background="{local:ThemeBrush #9A9F85BC}" Opacity=".78"/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Thumb" Property="Opacity" Value="1"/></Trigger>
                                                <Trigger Property="IsDragging" Value="True"><Setter TargetName="Thumb" Property="BorderBrush" Value="{local:ThemeBrush #E6E9DDF5}"/></Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton><RepeatButton Command="ScrollBar.PageDownCommand" Background="Transparent" BorderThickness="0"/></Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="Rail" Property="Height" Value="6"/>
                            <Setter TargetName="Rail" Property="VerticalAlignment" Value="Center"/>
                            <Setter TargetName="Rail" Property="Margin" Value="3,0"/>
                        </Trigger>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter TargetName="Rail" Property="Width" Value="6"/>
                            <Setter TargetName="Rail" Property="HorizontalAlignment" Value="Center"/>
                            <Setter TargetName="Rail" Property="Margin" Value="0,3"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="ContextMenu">
        <Setter Property="FontFamily" Value="{x:Static local:DockAppearance.UiFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{local:ThemeBrush #E9E0F2}"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="HasDropShadow" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Style="{StaticResource MenuPlate}">
                        <ScrollViewer CanContentScroll="True" VerticalScrollBarVisibility="Auto"
                            HorizontalScrollBarVisibility="Disabled" MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}">
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="MenuItem">
        <Setter Property="FontFamily" Value="{x:Static local:DockAppearance.UiFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{local:ThemeBrush #E9E0F2}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding" Value="8,7"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid>
                        <Border x:Name="Row" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="20"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter x:Name="Icon" ContentSource="Icon" Width="14" Height="14"
                                    HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                <TextBlock x:Name="Check" Text="✓" Foreground="{local:ThemeBrush #BDEDE5}" Visibility="Collapsed" VerticalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}" Foreground="{local:ThemeBrush #AE9FBD}"
                                    Margin="20,0,0,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="Arrow" Grid.Column="3" Text="›" FontSize="19" Visibility="Collapsed"
                                    HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <Popup x:Name="PART_Popup" Placement="Right" HorizontalOffset="-2" VerticalOffset="-7"
                            AllowsTransparency="True" Focusable="False" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                            PopupAnimation="Fade">
                            <Border Style="{StaticResource MenuPlate}">
                                <ScrollViewer CanContentScroll="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                                    MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" KeyboardNavigation.TabNavigation="Cycle"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Row" Property="Background" Value="{x:Static local:DockAppearance.ButtonHover}"/>
                            <Setter TargetName="Row" Property="BorderBrush" Value="{local:ThemeBrush #38DACDEC}"/>
                        </Trigger>
                        <Trigger Property="IsSubmenuOpen" Value="True">
                            <Setter TargetName="Row" Property="Background" Value="{x:Static local:DockAppearance.ButtonHover}"/>
                        </Trigger>
                        <Trigger Property="HasItems" Value="True"><Setter TargetName="Arrow" Property="Visibility" Value="Visible"/></Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Row" Property="Opacity" Value="0.4"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="Separator">
        <Setter Property="Template">
            <Setter.Value><ControlTemplate TargetType="Separator"><Border Height="1" Margin="12,5" Background="{local:ThemeBrush #2EDACDEC}"/></ControlTemplate></Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator" BasedOn="{StaticResource {x:Type Separator}}"/>
</ResourceDictionary>
